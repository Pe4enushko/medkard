# Журнал ревизий и эксплуатационных изменений

Журнал фиксирует изменения, которые влияют на сопоставимость результатов
аудита: смену логики проверок, источников данных, моделей и массовый переаудит.
Дата реализации и дата фактической выкатки записываются отдельно — до
стендовой/боевой проверки изменение нельзя считать применённым.

Правила ведения:

- даты — ISO `YYYY-MM-DD`;
- одна строка — одно проверяемое изменение;
- указывать коммит или ветку, конфигурацию стенда и результат проверки;
- массовый переаудит записывать отдельной строкой с числами до/после, поскольку
  он обновляет `updated_at` и отдаёт исправленные карты потребителям как свежие.

## Логика аудита и эксплуатационные прогоны

| Дата | Изменение | Состояние и проверка | Ссылка |
|---|---|---|---|
| 2026-08-20 | Diagnosis-ReAct заменён на ациклический устойчивый граф: четыре аспекта, программные источники чанков, критерии качества и fail-open реранкер | Реализовано локально; 79 изолированных тестов графа и переаудита проходят. Стендовая приёмка не выполнена | `d95772e`, ветка `diagnosis-graph` |
| 2026-08-20 | Добавлен офлайн-переаудит накопленных broken-карт с группировкой по организациям и отдельной сводкой `fixed` / `ignored` / `still broken` | Реализовано; DB/LLM-тесты подготовлены, но должны быть запущены на стенде | `5fdc48a`, ветка `diagnosis-graph` |
| 2026-08-22 | Тип визита определяется таблицей совпадений по частям кода (`validator._CODE_RULES`, сверена с 804н скриптом `scripts/check-nmu-classifier.py`), а не по одной группе `B01.070.*`; код, о котором таблица молчит, больше не глушит разбор наименования услуги | Реализовано на `dev`; юнит-тесты зелёные. **Влияет на сопоставимость**: на боевых картах поликлиники выбиралось 8 правил из 42, теперь 17–23 — число формальных замечаний и токенов на карту вырастет. Стендовая приёмка не выполнена | ветка `dev` |
| 2026-08-22 | Возраст пациента читается одним разбором (`parsers.json_parser.patient_age`): `AGE=0` — младенец, а не «нет данных»; нечитаемый возраст оставляет только правила `age_group=all`; граница «дети/взрослые» в подборе клинреков 15 → 18 лет | Реализовано на `dev`; юнит-тесты зелёные. Детские КР снова доступны 16–17-летним, младенцы перестали получать взрослые КР | ветка `dev` |
| 2026-08-22 | Диспансерный приём (`B04.*.001`) отделён от профилактического осмотра (`B04.*.002`) | Реализовано на `dev`; юнит-тесты зелёные, таблица сверена с 804н. **Влияет на сопоставимость**: на диспансерных приёмах исчезают четыре флага `ПРОФ_ВЗРОСЛЫЙ_*` и появляется `ДИСПАНСЕРНОЕ_НАБЛЮДЕНИЕ_НЕ_ОТРАЖЕНО` | ветка `dev` |
| 2026-08-22 | ICD-чекер переведён с ReAct-агента на двухэтапный конвейер: этап 1 — один вызов с полным перечнем КР отбирает до трёх гипотез, этап 2 — разделы читаются кодом, каждая гипотеза судится отдельным вызовом. Отбор КР по совпадению начала кода с кодом врача (`_select_manifest_rows`) снят: он выбрасывал 519 рекомендаций из 558 и делал невозможной замену кода из другого раздела МКБ. `suggested_code` теперь обязан принадлежать той рекомендации, по тексту которой судили | Реализовано на `dev`; 12 юнит-тестов контура зелёные. **Влияет на сопоставимость**: меняется и состав рекомендаций по кодам, и стоимость — перечень КР (≈18 тыс. токенов) отправляется один раз вместо переотправки на каждом шаге ReAct. Стендовая приёмка не выполнена | ветка `dev` |
| 2026-08-22 | Замечание ICD переформулировано в рекомендацию: `[РЕКОМЕНДАЦИЯ ПО КОДУ МКБ]` вместо `[ОШИБКА КОДИРОВАНИЯ МКБ]`, промпт просит предложить более подходящий код, а не выносить вердикт | Реализовано на `dev`. Порог уверенности ≥ 8 сохранён — меняется формулировка, а не объём | ветка `dev` |
| 2026-08-22 | Прогон diagnosis graph по рекомендованному ICD-коду убран | Реализовано на `dev`. **Влияет на сопоставимость**: с карты, где чекер предложил код, исчезает второй набор замечаний (на `I67.8`/`I69.3` их было 24 частично совпадающих) и связанный расход — карта на `I78.1`→`D22.9` стоила 200 345 токенов | ветка `dev` |
| 2026-08-22 | Отказ ICD-чекера больше не ломает карту: формальный и диагнозный контуры доезжают, `icd_check_result` пишется как SQL NULL. Лимиты ReAct-агента возвращены к 12/8/12/4 из `.env.example` | Реализовано на `dev`; красный на `dev` тест `test_call_agent_retries_recursion_in_compact_mode` погашен. **Влияет на сопоставимость**: часть карт, ранее уезжавших в broken, теперь завершается без ICD-раздела | ветка `dev` |
| — | Выкладка diagnosis-графа и Qwen3-Reranker на стенд | Заполнить: дата, версия модели, число карт в выборке, доля замечаний с источниками, ошибки деградации и время | — |
| — | `fix-broken.py --all` на стенде | Заполнить: broken до запуска; исправлено; ушло в ignored; осталось broken с разделением diagnosis/ICD | — |

При сравнении отчётов до и после выкатки учитывать, что аспект `criteria`
увеличивает число диагноз-замечаний, а исправленные карты возвращаются в
выгрузки с новым `updated_at`.

## Справочники и нормативная база

Фактические обновления клинических рекомендаций, лекарственных реестров и
формальных правил добавляются сюда отдельными строками только после загрузки и
проверки на стенде.

| Дата | Источник | Версия/срез | Как применено | Проверка |
|---|---|---|---|---|
| — | Клинические рекомендации | Заполнить по `guidelines` и последнему успешному `ingest_runs` | — | — |
| — | Лекарственный реестр | Заполнить по фактически используемому реестру и дате импорта | — | — |
| — | Формальные правила | Заполнить по `rules.json` и первоисточникам НПА | — | — |
