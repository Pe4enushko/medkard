"""Эвалы поиска по реестру лекарств.

Устройство:
    base.py        — доступ к Postgres: запись только в pg_temp, дальше read-only;
    corpus.py      — чтение реестра ГРЛС и производные тексты записи;
    embeddings.py  — поход к эндпоинту эмбеддингов;
    methods/       — по классу на способ поиска, общий базовый SearchMethod;
    datasets/      — по классу на набор запросов (синтетика и ручной набор);
    run.py         — CLI, который всё это связывает.

Зачем разнесено: способ поиска и набор запросов меняются независимо. Добавить
метод = добавить класс в methods/ и строчку в реестр; добавить набор запросов =
класс в datasets/. Ни то, ни другое не трогает харнесс.
"""
