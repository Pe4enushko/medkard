# LLM / embeddings
openai
instructor
pydantic

# LangChain (RAG agent)
langchain==1.3.16
langgraph==1.2.11
langchain-openai==1.6.0
langchain-core==1.6.0

# Sentence-transformers (optional: only needed when EMBEDDING_PROVIDER=st)
sentence-transformers
sentence-transformers[onnx-gpu]
optimum

# FastEmbed (optional: only needed when EMBEDDING_PROVIDER=fastembed)
fastembed

# PDF ingestion
pymupdf
tabula-py
langchain-text-splitters

# Vector store (asyncpg path)
asyncpg
pgvector

# Storage (psycopg3 path)
psycopg[binary]
psycopg-pool

# NLP / BM25
natasha
rank-bm25
numpy

# Utilities
python-dotenv
openpyxl

# Pull API
fastapi
uvicorn

# Tests
pytest
pytest-asyncio
pytest_asyncio
httpx
